# Настройка автоматического деплоя в Railway

Railway **по умолчанию** автоматически собирает новый билд при каждом push в подключённый репозиторий. Но нужно убедиться, что всё настроено правильно.

## ✅ Проверка автоматического деплоя

### 1. Проверьте подключение репозитория

1. Откройте [Railway Dashboard](https://railway.app/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Source**
4. Убедитесь, что:
   - ✅ Репозиторий подключён: `akhromovRT/pass24-bot-prime`
   - ✅ Ветка: `main`
   - ✅ **Auto-Deploy** включён (должна быть галочка)

### 2. Если Auto-Deploy выключен

1. В **Settings** → **Source**
2. Включите **"Auto-Deploy"** (переключатель должен быть активен)
3. Railway будет автоматически деплоить при каждом push в `main`

## 🔄 Как работает автоматический деплой

1. **Вы делаете push в GitHub:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```

2. **Railway автоматически:**
   - Обнаруживает изменения (через webhook от GitHub)
   - Запускает новую сборку (Build)
   - Останавливает старый контейнер
   - Запускает новый контейнер с обновлённым кодом

3. **Время деплоя:** обычно 2-5 минут

## 📊 Мониторинг деплоя

### В Railway Dashboard:

1. **Deployments** — показывает все деплои:
   - 🟢 Зелёный = успешно
   - 🔴 Красный = ошибка
   - 🟡 Жёлтый = в процессе

2. **Logs** — логи в реальном времени:
   - Видно процесс сборки
   - Видно запуск контейнера
   - Видно логи работы бота

### Проверка после push:

1. Сделайте push в репозиторий
2. Откройте Railway Dashboard → **Deployments**
3. Должен появиться новый деплой со статусом "Building" или "Deploying"
4. Через несколько минут статус изменится на "Active" (зелёный)

## ⚙️ Настройки деплоя (опционально)

### Branch для деплоя:

По умолчанию Railway деплоит из ветки `main`. Если нужно изменить:

1. **Settings** → **Source** → **Branch**
2. Выберите нужную ветку

### Build Command (если нужен):

Обычно Railway автоматически определяет команду сборки. Если нужно указать вручную:

1. **Settings** → **Deploy**
2. **Build Command:** `pip install -r requirements.txt`
3. **Start Command:** `python telegram_main.py`

### Переменные окружения:

Railway автоматически перезапускает сервис при изменении переменных окружения.

## 🚨 Если деплой не запускается автоматически

### Проверьте:

1. **GitHub Webhook:**
   - Railway должен иметь доступ к репозиторию
   - Проверьте в GitHub: Settings → Webhooks → должен быть webhook от Railway

2. **Auto-Deploy включён:**
   - Settings → Source → Auto-Deploy должен быть активен

3. **Права доступа:**
   - Railway должен иметь доступ к репозиторию
   - Если репозиторий приватный — нужен GitHub token

### Ручной деплой (если нужно):

Если автоматический деплой не работает, можно запустить вручную:

1. В Railway Dashboard → **Deployments**
2. Нажмите **"Redeploy"** или **"Deploy"**
3. Выберите коммит для деплоя

## 📝 Рекомендации

1. **Всегда делайте push в `main`** — Railway деплоит из этой ветки
2. **Проверяйте логи после деплоя** — убедитесь, что бот запустился
3. **Тестируйте после деплоя** — отправьте сообщение боту в Telegram

---

**Итог:** Railway автоматически собирает новый билд при каждом push в `main`. Просто делайте `git push` и Railway всё сделает! 🚀
